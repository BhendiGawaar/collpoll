# collpoll

Do not forget to change the path of datafile according to file location.

Flow:
reads the customers into customer class object one by one
claculates distance
uses the binary search to find the position of customer object to be inserted.
prints the list
